import vert from './vert.js';
import compute from './compute.js';
import frag from './frag.js';
const demo6 = {
    vert,
    compute,
    frag,
    init: async (points) => {
    },
    update: (points) => {

    }
}

export default demo6;